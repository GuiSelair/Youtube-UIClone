import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
      flex: 1
    },
    body: {
      flex: 1,
    },
    lineSeparator: {
      height: 0.8, 
      backgroundColor: "#cccccc"
    }
  });